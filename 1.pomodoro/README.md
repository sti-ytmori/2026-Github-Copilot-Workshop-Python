# 1.pomodoro 起動手順

## 前提
- Python 3 が利用できること
- Flask がインストール済みであること

## 起動
1. `cd 1.pomodoro`
2. `python3 app.py`

## 動作確認
- ブラウザで `http://localhost:8000` を開く
- ページ表示後、開発者ツールのコンソールで `timer.js loaded` が表示されること
