# ポモドーロタイマー Webアプリケーション アーキテクチャ案

## 1. 目的
本ドキュメントは、Flask と HTML/CSS/JavaScript で実装するポモドーロタイマーの設計方針を定義する。
目標は以下の両立。
- UIモックに忠実な体験を実現する
- 実装初期はシンプルに保つ
- 将来拡張しやすくする
- ユニットテストしやすくする

## 2. 基本方針
- 初期フェーズは クライアント主導タイマー とし、Flask は薄い配信層と最小APIを担当する
- タイマーのコアロジックは副作用を減らして分離し、純粋関数中心で実装する
- UI更新とドメインロジックを分離し、保守性とテスト性を高める

## 3. レイヤー構成
### 3.1 プレゼンテーション層
- HTML: 単一画面で構成
  - タイトル
  - モード表示 (作業中 / 休憩)
  - 円形プログレス
  - 残り時間
  - 開始 / リセット
  - 今日の進捗 (完了数 / 集中時間)
- CSS:
  - デザイントークンを変数化 (色、余白、角丸、影)
  - モックに近いカードUIとボタンを再現
  - モバイルとデスクトップ双方で崩れないレスポンシブを確保
- JavaScript:
  - 状態管理
  - タイマー進行
  - 描画更新
  - ユーザー入力イベント処理

### 3.2 アプリケーション層
- タイマー状態機械を定義
  - 状態: idle, running, paused, completed
  - イベント: start, pause, resume, reset, tick, finish
- 状態変更に応じて表示を更新する単方向の流れを採用

### 3.3 サーバー層 (Flask)
- 役割:
  - 画面配信
  - 静的ファイル配信
  - 必要最小限のAPI提供
- 方針:
  - ルートは薄く保つ
  - ビジネスロジックはサービス層へ委譲

## 4. タイマー実装方針
### 4.1 時間計測
- 毎秒デクリメント方式ではなく、終了予定時刻ベースで残り時間を再計算する
- 期待効果:
  - タブ非アクティブ時やスリープ復帰時のズレを最小化

### 4.2 円形プログレス
- SVGリングで実装し、進捗率から stroke-dashoffset を計算して描画する
- 進捗率は状態から一元計算し、リセットやモード切替時の不整合を防ぐ

### 4.3 モード管理
- モードを設定値として定義
  - work (25分)
  - short_break (5分)
  - long_break (15分)
- 長休憩の発生周期も設定値化し、将来の設定画面追加に備える
  - long_break_interval (4)

## 5. データ保存方針
- 初期はブラウザ保存を採用
  - 当日完了数
  - 集中時間
- 日付キーで管理し、日跨ぎ時に自動で集計を分離
- 保存処理は抽象化し、将来サーバー保存やDBに置換しやすくする

## 6. API最小案
- 設定取得 API
  - 作業時間、休憩時間、長休憩周期を返す
- 当日進捗取得 API
  - 完了数、集中時間を返す
- セッション完了送信 API
  - 作業セッション完了時の進捗更新を受け付ける

初期はサーバー側を簡略化してもよいが、インターフェースを先に定義しておく。

## 7. 推奨ディレクトリ構成
本リポジトリのルールに従い、ポモドーロ関連は 1.pomodoro 配下へ配置する。

- 1.pomodoro/
  - app.py (Flaskエントリ)
  - templates/
    - index.html
  - static/
    - css/
      - style.css
    - js/
      - timer.js
      - state_machine.js
      - storage.js
  - domain/
    - timer_logic.py (必要に応じてサーバー側ロジックを追加)
  - services/
    - progress_service.py
  - tests/
    - test_timer_logic.py
    - test_state_machine.py
    - test_progress_service.py
    - test_routes.py

## 8. ユニットテストしやすさのための設計要件
### 8.1 純粋関数化
- 残り時間計算、完了判定、進捗率計算を純粋関数として切り出す
- 入出力ベースで境界値テストを行えるようにする

### 8.2 Clock抽象
- 現在時刻取得を直接呼ばず、Clockインターフェース経由にする
- テストで固定時刻を注入し、再現性を担保する

### 8.3 状態遷移の独立
- 状態機械をDOM操作から分離
- イベント列に対する状態遷移テストを可能にする

### 8.4 Repository抽象
- 保存先を直接 localStorage に依存させない
- 本番実装とインメモリ実装を差し替えてテストする

### 8.5 Flaskルートの薄化
- ルートは入力検証とレスポンス形成に専念
- ロジックはサービス層でテストする

### 8.6 設定オブジェクト化
- タイマー長や周期を設定として集約
- テストごとに差し替え可能にする

## 9. テスト戦略
- ドメインユニットテスト
  - 残り時間計算
  - 完了判定
  - 日跨ぎ判定
  - 集中時間累積
- 状態遷移テスト
  - start/pause/resume/reset/tick/finish の遷移検証
- サービステスト
  - 完了時の進捗更新
  - 保存処理呼び出し
- ルートテスト
  - 正常系
  - 入力異常系
  - 例外時レスポンス
- フロントロジックテスト
  - 状態から表示への変換
  - プログレス描画計算

## 10. 実装ステップ
1. Flask最小構成で画面配信
2. HTML/CSSでモックUIを再現
3. JavaScriptで状態機械と時間計測ロジックを実装
4. 円形プログレスを連動
5. 当日進捗の保存と表示を実装
6. APIを追加し、必要に応じてサーバー保存へ拡張

## 11. 将来拡張
- ユーザー認証
- DB永続化
- 履歴分析と統計表示
- 通知機能
- 設定画面

この構成により、初期実装のスピードを維持しつつ、将来拡張とテスト容易性を同時に確保できる。