# ポモドーロタイマー 実装機能一覧

このドキュメントは、[architecture.md](architecture.md) と UI モックをもとに、実装が必要な機能を整理したものです。

## 1. 初期リリースで必須（MVP）

1. 単一画面のタイマーUI
2. モード表示（作業中 / 休憩）
3. 残り時間表示（mm:ss）
4. 開始ボタン
5. リセットボタン
6. 円形プログレス表示（進捗率に応じてリング更新）
7. 今日の進捗表示（完了数 / 集中時間）
8. Flaskによる画面配信（HTML返却）
9. 静的ファイル配信（CSS/JS）
10. レスポンシブ対応（モバイル/デスクトップ）
11. 状態管理（idle, running, paused, completed）
12. 状態遷移イベント（start, pause, resume, reset, tick, finish）
13. 終了予定時刻ベースの時間計測
14. セッション完了時の進捗更新（完了数+1、集中時間加算）
15. ブラウザ保存（当日進捗の永続化）
16. 日付キーによる日跨ぎ分離（当日データ切替）
17. 設定値管理（work / short_break / long_break / 長休憩周期）

## 2. MVPで用意しておくと良い（拡張しやすさのため）

1. 保存処理の抽象化（localStorage直接依存を避ける）
2. タイマーロジックの純粋関数化
3. UI更新ロジックとドメインロジックの分離
4. Flaskルートの薄化（ロジックはサービス層へ委譲）
5. APIインターフェースの先行定義
6. エラーハンドリング（入力異常/例外時のレスポンス）

## 3. API実装対象（最小案）

1. 設定取得API
2. 当日進捗取得API
3. セッション完了送信API

## 4. テスト実装対象

1. ドメインテスト
   - 残り時間計算
   - 完了判定
   - 進捗率計算
   - 日跨ぎ判定
2. 状態遷移テスト（start/pause/resume/reset/tick/finish）
3. サービステスト（進捗更新、保存処理呼び出し）
4. ルートテスト（正常系/異常系/例外系）
5. フロントロジックテスト（状態から表示変換、プログレス計算）

## 5. モック準拠で明確化すべき仕様

1. 初期表示値（25:00、作業中、進捗初期値）
2. 実行中のボタン挙動（開始→一時停止/再開の扱い）
3. セッション完了時のモード遷移（作業↔休憩）
4. 長休憩の挿入タイミング（例: 作業4回ごと）
5. リセット時の状態同期（時間・リング・表示文言を整合）
