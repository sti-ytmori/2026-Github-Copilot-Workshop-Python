# ポモドーロタイマー 段階的実装計画

## 目的
添付モック、[architecture.md](architecture.md)、[features.md](features.md) を前提に、
実装順序・依存関係・完了条件を明確にした段階的な開発計画を定義する。

## フェーズ0: 仕様確定（最短）
### 目的
実装前に曖昧点を固定し、後戻りコストを下げる。

### 決定項目
1. 開始ボタン挙動（開始→一時停止→再開）
2. セッション完了時のモード遷移（作業↔休憩）
3. 長休憩挿入タイミング（例: 作業4回ごと）
4. 初期表示値（25:00、作業中、進捗初期値）
5. リセット時の同期対象（時間、リング、文言、状態）

### 完了条件
- [features.md](features.md) の「モック準拠で明確化すべき仕様」が全て確定している。

## フェーズ1: Flask最小構成（配信基盤）
### 目的
画面配信と静的ファイル配信の土台を作る。

### 実装対象
1. Flask エントリポイント実装（app.py）
2. 画面配信ルート（index）
3. 静的ファイル配信（CSS/JS）
4. 最低限の起動手順整備

### 完了条件
- ブラウザでページ表示できる。
- CSS と JS が読み込まれる。

### 対応機能
- MVP: 8, 9

## フェーズ2: UIモック再現（静的UI）
### 目的
まず見た目をモック準拠で完成させる。

### 実装対象
1. 単一画面レイアウト
2. モード表示、残り時間表示
3. 開始・リセットボタン
4. 今日の進捗パネル
5. モバイル/デスクトップのレスポンシブ調整

### 完了条件
- 画面構造と見た目がモックに概ね一致する。
- レイアウト崩れが主要ブレークポイントで発生しない。

### 対応機能
- MVP: 1, 2, 3, 4, 5, 7, 10

## フェーズ3: タイマーコア実装（状態機械 + 時間計測）
### 目的
UIから分離したドメインロジックを確立する。

### 実装対象
1. 状態管理（idle, running, paused, completed）
2. 状態遷移イベント（start, pause, resume, reset, tick, finish）
3. 終了予定時刻ベースの残り時間計算
4. 設定値管理（work, short_break, long_break, long_break_interval）

### 完了条件
- イベント列に対して状態遷移が期待通りに動く。
- タブ非アクティブ復帰時も時間ずれが小さい。

### 対応機能
- MVP: 11, 12, 13, 17

## フェーズ4: UI連動（動作するタイマー）
### 目的
ユーザー操作でタイマーが正しく動く体験を完成させる。

### 実装対象
1. 開始/一時停止/再開/リセットのUIイベント接続
2. 残り時間表示のリアルタイム更新
3. モード表示の状態連動
4. 円形プログレス更新（進捗率→リング描画）

### 完了条件
- 操作に対して表示・状態・リングが整合する。
- リセット時に全要素が初期化される。

### 対応機能
- MVP: 4, 5, 6

## フェーズ5: 当日進捗の永続化
### 目的
実利用に必要な当日実績の保存を実現する。

### 実装対象
1. セッション完了時の進捗更新（完了数+1、集中時間加算）
2. ブラウザ保存（localStorage）
3. 日付キーによる日跨ぎ分離
4. 保存処理の抽象化（将来の置換に備える）

### 完了条件
- リロード後も当日進捗が保持される。
- 日付変更で当日データが切り替わる。

### 対応機能
- MVP: 14, 15, 16
- 拡張しやすさ: 1

## フェーズ6: 最小API実装（拡張準備）
### 目的
クライアント主導を維持しつつAPI境界を先行定義する。

### 実装対象
1. 設定取得API
2. 当日進捗取得API
3. セッション完了送信API
4. Flaskルートの薄化（サービス層へ委譲）
5. 異常系レスポンス整備

### 完了条件
- 3 API が契約どおり動作する。
- ルート層に過剰なロジックがない。

### 対応機能
- API最小案: 1, 2, 3
- 拡張しやすさ: 4, 5, 6

## フェーズ7: テスト整備と品質固定
### 目的
回帰防止とリリース可能性を担保する。

### 実装対象
1. ドメインテスト（残り時間計算、完了判定、進捗率計算、日跨ぎ判定）
2. 状態遷移テスト（start/pause/resume/reset/tick/finish）
3. サービステスト（進捗更新、保存処理呼び出し）
4. ルートテスト（正常系/異常系/例外系）
5. フロントロジックテスト（表示変換、進捗計算）

### 完了条件
- 主要ユースケースがテストで保護される。
- 既知の重大不具合がない。

## マイルストーン提案
1. マイルストーンA: フェーズ1〜2（表示可能なモック準拠UI）
2. マイルストーンB: フェーズ3〜4（操作可能なタイマー）
3. マイルストーンC: フェーズ5〜6（進捗保存とAPI境界）
4. マイルストーンD: フェーズ7（テスト整備完了）

## 1タスクの推奨粒度
- 半日〜1日で完了する単位に分割する。
- 各タスクに「完了条件」を必ず付与する。
- 1タスク1PRでレビュー可能性を高める。

## 初動（次の実装開始点）
1. フェーズ0の仕様確定を完了する。
2. フェーズ1のFlask最小構成に着手する。
3. フェーズ2でモック準拠の静的UIを完成させる。
